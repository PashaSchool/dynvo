"""MCP tool modules. Import these to register their FastMCP tools.

The server's ``main()`` lazy-imports both modules so tools self-register
on the shared ``mcp`` instance from :mod:`faultlines_mcp.server`.
"""
